package nxpense.message;

/**
 *  The {@code CustomResponseHeader} enum holds the name of custom headers that could be sent back to the client within a ResponseEntity object
 */
public enum CustomResponseHeader {

    SERVERSIDE_VALIDATION_ERROR_MSG
}
